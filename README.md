# kg-lb-converter

Minimal Python CLI for kilogram and pound conversions.

## Usage

Make the script executable if needed:

```bash
chmod +x converter
```

Run it with a single number:

```bash
./converter 30
```

Example output:

```text
30 kg = 66.14 lb
30 lb = 13.61 kg
```
